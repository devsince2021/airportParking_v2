<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com?plugins=forms,typography,aspect-ratio,line-clamp"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              clifford: '#da373d',
            },
          },
        },
      };
    </script>
  </head>
  <body>
    <% if (frame) { %>
    <div class="w-screen min-h-screen flex bg-red-100">
      <div class="w-64 bg-red-200 flex flex-col items-center">
        <div
          class="w-full h-32 mt-5 flex flex-col items-center justify-center bg-red-300"
        >
          company
        </div>
        <div class="mt-5 flex flex-col w-full pl-5">
          <div class="py-2">
            <a href="/">대시보드</a>
          </div>
          <div class="py-2">
            <a href="/reservation">예약등록</a>
          </div>
        </div>
      </div>
      <div class="flex flex-col w-full">
        <div class="flex px-5 bg-red-200 py-5 justify-between">
          <div><%= title %></div>
          <div class="pr-5">
            <form action="/users/logout">
              <button>logout</button>
            </form>
          </div>
        </div>
        <%- body %>
      </div>
    </div>
    <% } else { %> <%- body %> <% } %>
  </body>
</html>
