import { mockValidUser } from './users.entity';

// it doesn't work;;
// export const UsersService = jest.fn().mockResolvedValue({
//   createUser: jest.fn().mockResolvedValue(mockValidUser()),
// });
